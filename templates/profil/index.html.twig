{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!
{% endblock %}

{% block body %}
	<div class="container rounded bg-white mt-5 mb-5">
		<div class="row">
			<div class="col-md-5 border-right">
				<div class="p-3 py-5">
					<div class="d-flex justify-content-between align-items-center mb-3">
						<h4 class="text-right">Profil</h4>
					</div>
					<div class="row mt-2">
						<div class="col-md-12">
							<label class="labels">Email/Username :
								{{user.email}}</label>
						</div>
					</div>
					<div class="row mt-2">
						<div class="col-md-6">
							<label class="labels">Nom :
								{{user.nom}}</label>
						</div>
						<div class="col-md-6">
							<label class="labels">Prénom :
								{{user.prenom}}</label>
						</div>
					</div>

					<div class="mt-5 text-center">
						<a href="{{path('edit_password')}}" class="button">Changer le mot de Passe</a>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
