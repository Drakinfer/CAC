{% extends 'base.html.twig' %}

{% block title %}ServiceEquipe
{% endblock %}

{% block body %}
	<h1>ServiceEquipe</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ service_equipe.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ service_equipe.name }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('equipe_index') }}">back to list</a>

	<a href="{{ path('service_equipe_edit', {'id': service_equipe.id}) }}">edit</a>

	{{ include('service_equipe/_delete_form.html.twig') }}
{% endblock %}
