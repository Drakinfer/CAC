<form method="post" action="{{ path('service_equipe_delete', {'id': service_equipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_equipe.id) }}">
	<button class="button">Supprimer</button>
</form>
