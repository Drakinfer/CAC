{% extends 'base.html.twig' %}

{% block title %}LigneProgramme{% endblock %}

{% block body %}
    <h1>LigneProgramme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_programme.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ ligne_programme.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligne_programme_index') }}">back to list</a>

    <a href="{{ path('ligne_programme_edit', {'id': ligne_programme.id}) }}">edit</a>

    {{ include('ligne_programme/_delete_form.html.twig') }}
{% endblock %}
