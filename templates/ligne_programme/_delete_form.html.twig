<form method="post" action="{{ path('ligne_programme_delete', {'id': ligne_programme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_programme.id) }}">
    <button class="btn">Delete</button>
</form>
