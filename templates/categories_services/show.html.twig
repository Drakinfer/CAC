{% extends 'base.html.twig' %}

{% block title %}CategoriesServices{% endblock %}

{% block body %}
    <h1>CategoriesServices</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categories_service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categories_service.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categories_services_index') }}">back to list</a>

    <a href="{{ path('categories_services_edit', {'id': categories_service.id}) }}">edit</a>

    {{ include('categories_services/_delete_form.html.twig') }}
{% endblock %}
