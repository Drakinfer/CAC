<form method="post" action="{{ path('paragraphe_temoignage_delete', {'id': paragraphe_temoignage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paragraphe_temoignage.id) }}">
    <button class="btn">Delete</button>
</form>
