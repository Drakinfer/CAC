{% extends 'base.html.twig' %}

{% block title %}ParagrapheTemoignage{% endblock %}

{% block body %}
    <h1>ParagrapheTemoignage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paragraphe_temoignage.id }}</td>
            </tr>
            <tr>
                <th>Paragraphe</th>
                <td>{{ paragraphe_temoignage.paragraphe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paragraphe_temoignage_index') }}">back to list</a>

    <a href="{{ path('paragraphe_temoignage_edit', {'id': paragraphe_temoignage.id}) }}">edit</a>

    {{ include('paragraphe_temoignage/_delete_form.html.twig') }}
{% endblock %}
