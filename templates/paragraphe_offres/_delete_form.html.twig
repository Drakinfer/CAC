<form method="post" action="{{ path('paragraphe_offres_delete', {'id': paragraphe_offre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paragraphe_offre.id) }}">
    <button class="btn">Delete</button>
</form>
