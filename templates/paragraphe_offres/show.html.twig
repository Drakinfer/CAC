{% extends 'base.html.twig' %}

{% block title %}ParagrapheOffres{% endblock %}

{% block body %}
    <h1>ParagrapheOffres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paragraphe_offre.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ paragraphe_offre.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paragraphe_offres_index') }}">back to list</a>

    <a href="{{ path('paragraphe_offres_edit', {'id': paragraphe_offre.id}) }}">edit</a>

    {{ include('paragraphe_offres/_delete_form.html.twig') }}
{% endblock %}
